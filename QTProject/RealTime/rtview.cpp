#include "rtview.h"

RTView::RTView()
{
    /*
    struct View
    {
        enum Format
        {
            None = 0,
            Gray8,
            Rgb24,
            Rgba32,
        };

        const size_t width;
        const size_t height;
        const ptrdiff_t stride;
        const Format format;
        uint8_t * data;

        View();
        View(const View & view);
        View(size_t w, size_t h, ptrdiff_t s, Format f, void * d);


    };
    */
}
